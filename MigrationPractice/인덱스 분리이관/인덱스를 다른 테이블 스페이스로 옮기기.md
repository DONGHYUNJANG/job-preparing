# 인덱스를 다른 테이블 스페이스로 옮기기

---

> 개념 설명
> 
- **성능 분산**: 데이터와 인덱스를 물리적으로 다른 디스크(테이블스페이스)에 배치하여 I/O 경합을 줄입니다.
- **관리 편의성**: 인덱스만 따로 관리하면 백업이나 테이블스페이스 유지보수가 용이해집니다.
- **Rebuild 효과**: 이동 과정에서 `REBUILD` 명령어를 사용하므로, 인덱스 트리의 밸런스가 잡히고 빈 공간이 정리됩니다.

> 주의사항
> 
- **잠금(Lock)**: 일반적인 `REBUILD`는 테이블에 독점 잠금을 걸어 DML을 차단합니다. 서비스 중이라면 `ONLINE` 옵션이 필수입니다.
- **리소스**: 인덱스 재생성 시 CPU와 Temp 테이블스페이스, Undo 공간을 많이 사용합니다.

> 인덱스 종류 별 이관/분리 권장 여부
> 

| **인덱스 종류** | **이관/분리 권장** | **성능(I/O) 관점의 배치** | **이유** |
| --- | --- | --- | --- |
| **일반 B-Tree** | **적극 권장** | 별도 디스크 분리 | 가장 확실한 I/O 분산 효과 |
| **비트맵(Bitmap)** | **권장** | 별도 디스크 분리 | 대용량 분석 쿼리 병렬 처리 유리 |
| **파티션 인덱스** | **성능적으로 유리하나 확인은 필요** | 테이블과 인접 배치 | 관리 편의성 vs I/O 분산 사이의 선택 |
| **LOB 인덱스** | 성능적으로 유리하나 확인은 필요 | LOB 데이터와 같은 곳 | 물리적 종속성이 강함 (MOVE LOB 필요) |
| **IOT** | **불가능하나 보통 INDEX용 테이블 스페이스에 함께 관리한다.** | **데이터와 합체** | **테이블 자체가 인덱스이므로 분할 불가** |
| **Domain Index** | **주의** | 애플리케이션 특성 따름 | 오라클 텍스트 등 외부 엔진 연동 가능성 |

## 2. 사전준비

> UNDO 테이블 스페이스, TEMP 테이블 스페이스의 공간 여유 확인
> 

## 🛠️ 스키마 전체 인덱스 합계 계산

"총 얼마나 이동해야 하는가?"를 알고 싶을 때 사용합니다.

```jsx
SELECT 
    OWNER,
    COUNT(*) AS INDEX_COUNT,
    -- MB 단위로 변경하고 소수점을 4자리까지 확인
    ROUND(SUM(BYTES) / 1024 / 1024, 4) AS TOTAL_MB,
    -- KB 단위도 같이 확인
    ROUND(SUM(BYTES) / 1024, 2) AS TOTAL_KB
FROM DBA_SEGMENTS
WHERE OWNER = 'SH'
  AND SEGMENT_TYPE LIKE '%INDEX%'
GROUP BY OWNER;

SELECT 
    SEGMENT_TYPE, 
    COUNT(*) AS CNT,
    ROUND(SUM(BYTES) / 1024 / 1024, 2) AS MB_SUM
FROM DBA_SEGMENTS
WHERE OWNER = 'SH'
  AND SEGMENT_TYPE LIKE '%INDEX%'
GROUP BY SEGMENT_TYPE;
```

## 📊 자원 산정 가이드 (Rule of Thumb)

### 리소스 계산 공식

| 리소스 | 권장 확보 용량 | 비고 |
| --- | --- | --- |
| Permanent (INDEX TS) | 인덱스 크기 × **2배** | 기존 인덱스와 새 인덱스가 잠시 공존 |
| Temp TS | 인덱스 크기 × **1.5배** | 정렬(Sort) 작업용 공간 |
| Undo TS | 인덱스 크기 × **10% ~ 20%** | `ONLINE` 옵션 시 트랜잭션 양에 따라 가변 |

### 🛠️ 실제 예시 (인덱스 크기 10GB)

- **테이블스페이스 여유**: 10GB 이상 (새 인덱스가 들어갈 자리)
- **Temp 공간**: 약 15GB 확보 권장
- **Undo 공간**: 평소보다 2~5GB 정도 더 여유 있게 확보 (작업 중 DML이 많다면 더 필요)

### 🔍 TEMP/UNDO 디폴트 확인 SQL

#### 1) 디폴트 TEMP 테이블스페이스 이름 확인

```sql
SELECT PROPERTY_VALUE AS DEFAULT_TEMP_TS
FROM DATABASE_PROPERTIES
WHERE PROPERTY_NAME = 'DEFAULT_TEMP_TABLESPACE';
```

#### 2) TEMP 테이블스페이스 용량/사용량 확인

```sql
-- TEMP 전체 할당/사용/여유 (현재 사용량 기준)
SELECT
  tf.tablespace_name,
  ROUND(SUM(tf.bytes)/1024/1024/1024, 2) AS total_gb,
  ROUND(SUM(NVL(th.bytes_used,0))/1024/1024/1024, 2) AS used_gb,
  ROUND((SUM(tf.bytes)-SUM(NVL(th.bytes_used,0)))/1024/1024/1024, 2) AS free_gb
FROM dba_temp_files tf
LEFT JOIN v$temp_space_header th
  ON th.tablespace_name = tf.tablespace_name
 AND th.file_id = tf.file_id
GROUP BY tf.tablespace_name
ORDER BY tf.tablespace_name;
```

#### 3) 디폴트 UNDO 테이블스페이스 이름 확인

```sql
SHOW PARAMETER undo_tablespace;
```

#### 4) UNDO 테이블스페이스 용량/사용량 확인

```sql
-- UNDO 전체 할당량(데이터파일 합)
SELECT
  tablespace_name,
  ROUND(SUM(bytes)/1024/1024/1024, 2) AS total_gb
FROM dba_data_files
WHERE tablespace_name = (SELECT value FROM v$parameter WHERE name = 'undo_tablespace')
GROUP BY tablespace_name;

-- UNDO 사용량(대략치: 데이터파일 - free)
SELECT
  df.tablespace_name,
  ROUND(SUM(df.bytes)/1024/1024/1024, 2) AS total_gb,
  ROUND((SUM(df.bytes)-SUM(fs.bytes))/1024/1024/1024, 2) AS used_gb,
  ROUND(SUM(fs.bytes)/1024/1024/1024, 2) AS free_gb
FROM dba_data_files df
JOIN (
  SELECT tablespace_name, SUM(bytes) AS bytes
  FROM dba_free_space
  GROUP BY tablespace_name
) fs
  ON fs.tablespace_name = df.tablespace_name
WHERE df.tablespace_name = (SELECT value FROM v$parameter WHERE name = 'undo_tablespace')
GROUP BY df.tablespace_name;
```

## 2. 인덱스 이동 스크립트

## 🛠️ PL/SQL로 자동 실행 (고급)

한 번에 자동으로 실행하고 싶다면 아래 블록을 활용하세요.

> 일반 인덱스
> 

```sql
BEGIN
  FOR idx IN (
    SELECT OWNER, INDEX_NAME
    FROM DBA_INDEXES
    WHERE OWNER = 'SCOTT'
      AND INDEX_TYPE = 'NORMAL'
  )
  LOOP
    EXECUTE IMMEDIATE 'ALTER INDEX ' || idx.OWNER || '.' || idx.INDEX_NAME ||
                      ' REBUILD TABLESPACE NEW_INDEX_TS ONLINE';
  END LOOP;
END;
/
```

> 파티션 인덱스 
주의!!! 파티션 인덱스도 이동할 것인지 확인을 받을것!!!!
> 

```jsx
BEGIN
  FOR idx IN (
    -- SH 스키마 중 아직 TS20000IDX로 옮겨지지 않은 '인덱스 파티션'들을 조회
    SELECT INDEX_OWNER, INDEX_NAME, PARTITION_NAME
    FROM DBA_IND_PARTITIONS
    WHERE INDEX_OWNER = 'SH'
      AND TABLESPACE_NAME != 'TS20000IDX'
  )
  LOOP
    -- 파티션 단위로 REBUILD 실행
    EXECUTE IMMEDIATE 'ALTER INDEX ' || idx.INDEX_OWNER || '.' || idx.INDEX_NAME || 
                      ' REBUILD PARTITION ' || idx.PARTITION_NAME || 
                      ' TABLESPACE TS20000IDX ONLINE';
  END LOOP;
END;
/
```

> 비트맵 인덱스 이동
주의!!! 비트맵 인덱스도 이동할 것인지 확인을 받을것!!!!
비트맵 인덱스는 ONLINE으로 실행할 수 없기 때문에 베타적 락이 넓은 범위로 일어나므로 조심해야한다.
> 

```jsx
BEGIN
    FOR idx IN (
        SELECT OWNER, INDEX_NAME
        FROM DBA_INDEXES
        WHERE OWNER = 'SH'
          AND INDEX_TYPE = 'BITMAP'
    ) LOOP
        BEGIN
            -- ONLINE 키워드를 제거했습니다.
            EXECUTE IMMEDIATE 'ALTER INDEX ' || idx.OWNER || '.' || idx.INDEX_NAME || 
                              ' REBUILD TABLESPACE TS20000IDX';
            
            DBMS_OUTPUT.PUT_LINE('성공: ' || idx.INDEX_NAME);
        EXCEPTION
            WHEN OTHERS THEN
                DBMS_OUTPUT.PUT_LINE('실패: ' || idx.INDEX_NAME || ' - ' || SQLERRM);
        END;
    END LOOP;
END;
/
```

> 도메인 인덱스 이동
> 

```jsx
SELECT i.owner,
       i.index_name,
       i.table_name,
       c.column_name
FROM dba_indexes i
JOIN dba_ind_columns c
  ON i.index_name = c.index_name
WHERE i.index_name = 'SUP_TEXT_IDX';
```

> 지우고 새로 만드는 법
> 

```jsx
DROP INDEX SH.SUP_TEXT_IDX

alter user sh default tablespace ts20000idx;
 
CREATE INDEX SH.SUP_TEXT_IDX
ON SH.SUPPLEMENTARY_DEMOGRAPHICS(comments)
INDEXTYPE IS CTXSYS.CONTEXT;

alter user sh default tablespace <원래 사용하던 tablesapce)
```

> 리빌드 방법
> 

```jsx
alter user sh default tablespace ts20000idx;
 
ALTER INDEX SH.SUP_TEXT_IDX REBUILD;
alter user sh default tablespace <원래 사용하던 tablesapce)
```

> 인덱스 동작 확인
> 

```jsx
SELECT *
FROM SUPPLEMENTARY_DEMOGRAPHICS
WHERE CONTAINS(COMMENTS, 'Shopping') > 0;

SELECT owner, index_name, status
FROM dba_indexes
WHERE index_name = 'SUP_TEXT_IDX';
```

## ✅ 3. 인덱스가 잘 넘어갔는지 확인 SQL

### 1) 목표 테이블스페이스로 변경됐는지(DBA_SEGMENTS)

```sql
SELECT
    TABLESPACE_NAME, 
    COUNT(*) AS INDEX_COUNT,
    ROUND(SUM(BYTES) / 1024 / 1024, 2) AS SIZE_MB
FROM DBA_SEGMENTS
WHERE OWNER = 'SH'
  AND SEGMENT_TYPE LIKE '%INDEX%'
GROUP BY TABLESPACE_NAME; 
```

> 필수 확인 문서
> 

[Oracle TEXT INDEX 상태 확인 체크리스트](https://www.notion.so/Oracle-TEXT-INDEX-3280f3217fe880f3b66ac5181eafee46?pvs=21)

[LOB 인덱스 물리적 분리 및 TTS 가이드](https://www.notion.so/LOB-TTS-3270f3217fe8812ab173cb6548e1e4af?pvs=21)

[프로시져 버전](https://www.notion.so/3270f3217fe881a2b0ade2e0e47ef48d?pvs=21)

> 그외
> 

[**Oracle 비트맵 인덱스(Bitmap Index)의 제약 사항**](https://www.notion.so/Oracle-Bitmap-Index-3280f3217fe880f0ab24e76703641bc0?pvs=21)

[비트맵 인덱스(Bitmap Index)의 특징](https://www.notion.so/Bitmap-Index-3280f3217fe88083b4a1f415c3df6e2e?pvs=21)

[LOB인덱스를 옮길때 왜 몇번에 걸쳐서 옮겨야 하는 이유](https://www.notion.so/LOB-3280f3217fe88088abe3ebbe4fe526c4?pvs=21)

[LOB 인덱스에 대한 개념](https://www.notion.so/LOB-3280f3217fe88015b822d3de992ca531?pvs=21)

[**TEXT INDEX의 역할**](https://www.notion.so/TEXT-INDEX-3280f3217fe880549d01d465ed70065e?pvs=21)

[1️⃣ LOB INDEX의 진짜 역할](https://www.notion.so/1-LOB-INDEX-3280f3217fe8806e9071df2385e5685d?pvs=21)

[LOB인덱스를 이동시키는 3가지 방법](https://www.notion.so/LOB-3-3280f3217fe880c6b576c873f795fdf6?pvs=21)

[IOT의 이동](https://www.notion.so/IOT-3280f3217fe880fe9a02e8ea303d2082?pvs=21)

[LOB INDEX이동 실패 할시](https://www.notion.so/LOB-INDEX-3280f3217fe880ea944deee1ea6e65e8?pvs=21)