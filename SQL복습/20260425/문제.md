PostgreSQL을 활용하여 4개의 테이블을 연합하고 집계하는 카테고리별 매출 분석 문제입니다.
------------------------------
## [데이터베이스 스키마 및 샘플 데이터]

-- 1. 카테고리 테이블CREATE TABLE categories (
    category_id INT PRIMARY KEY,
    category_name VARCHAR(50) NOT NULL
);
-- 2. 상품 테이블CREATE TABLE products (
    product_id INT PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    category_id INT REFERENCES categories(category_id)
);
-- 3. 주문 테이블CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    order_date DATE NOT NULL,
    customer_id INT
);
-- 4. 주문 상세 테이블CREATE TABLE order_items (
    order_item_id INT PRIMARY KEY,
    order_id INT REFERENCES orders(order_id),
    product_id INT REFERENCES products(product_id),
    quantity INT NOT NULL
);
-- 데이터 삽입INSERT INTO categories VALUES 
(1, 'Electronics'), (2, 'Clothing'), (3, 'Home & Kitchen');
INSERT INTO products VALUES 
(101, 'Laptop', 1200.00, 1),
(102, 'Smartphone', 800.00, 1),
(103, 'T-Shirt', 25.00, 2),
(104, 'Jeans', 50.00, 2),
(105, 'Blender', 150.00, 3);
INSERT INTO orders VALUES 
(501, '2024-04-01', 1),
(502, '2024-04-02', 2),
(503, '2024-04-03', 3);
INSERT INTO order_items VALUES 
(1, 501, 101, 1), -- Laptop 1개
(2, 501, 103, 2), -- T-Shirt 2개
(3, 502, 102, 2), -- Smartphone 2개
(4, 502, 105, 1), -- Blender 1개
(5, 503, 104, 3), -- Jeans 3개
(6, 503, 105, 2); -- Blender 2개

------------------------------
## [SQL 문제]
위의 네 가지 테이블(categories, products, orders, order_items)을 조인하여 각 카테고리별 매출액과 전체 매출에서 해당 카테고리가 차지하는 비중(%)을 구하는 쿼리를 작성하세요.
요구사항:

   1. 출력 컬럼은 category_name, category_sales(카테고리별 매출 합계), sales_percentage(매출 비중) 순으로 표시하세요.
   2. sales_percentage는 전체 매출액 대비 해당 카테고리의 매출액 비율을 계산하며, 소수점 둘째 자리까지 반올림하여 표시하세요.
   3. 매출액이 높은 카테고리부터 내림차순으로 정렬하세요.
   4. Window Function(윈도우 함수)을 활용하여 전체 매출액을 산출하고 비중을 계산하세요.

작성하신 SQL 쿼리가 의도대로 작동하는지 확인을 위해 결과값을 요청하시겠어요?

